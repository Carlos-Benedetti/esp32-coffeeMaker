import { CoffeeMakerController } from './coffee-maker-controller';

describe('CoffeeMakerController', () => {
  it('should create an instance', () => {
    expect(new CoffeeMakerController()).toBeTruthy();
  });
});
